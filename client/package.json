{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "img2weibo": "^1.0.5",
    "react": "^16.8.3",
    "react-dom": "^16.8.3",
    "react-scripts": "^2.1.5",
    "react-snap": "^1.23.0",
    "use-debounce": "^1.0.0"
  },
  "devDependencies": {
    "react-app-rewired": "^2.0.0",
    "react-app-rewire-stylus-modules": "^0.0.2"
  },
  "scripts": {
    "start": "react-app-rewired start",
    "build:react": "react-app-rewired build",
    "build:snap": "react-snap",
    "build": "npm run build:react && npm run build:snap",
    "postbuild": "rm -rf ../docs && mv build ../docs",
    "test": "react-app-rewired test",
    "eject": "react-app-rewired eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
